package com.sudox.android.common.enums

enum class ConnectState {
    CONNECTED,
    CONNECT_ERROR,
    DISCONNECTED,
    RECONNECTED,
    ATTACKED,
    MISSING_TOKEN,
    CORRECT_TOKEN,
    WRONG_TOKEN
}